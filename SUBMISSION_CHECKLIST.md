# SUBMISSION_CHECKLIST — IEEE Access (Fable pass, 2026-07-14)
> Status per item: ✅ done | ⚠️ needs your action | ⛔ blocks submission

## Manuscript
- ✅ Full manuscript in `paper/access.tex` (official 2026-05-13 IEEE Access template; booktabs added).
- ✅ Abstract 150–250 words, no refs/abbreviations-first-use violations; keywords alphabetized.
- ✅ Every quantitative claim regenerates from `src/paper_numbers.py` → `results/paper_numbers.json`;
  fresh sub-agent verification pass run over every claim cluster (see REVIEW_ME_FABLE.md for outcomes).
- ✅ fab_inference.md reframes folded in: no "flat in single-turn" blanket claim (pooled FE trend
  reported); no universal "anon ≈ professor" (McNemar table 2, both signs reported); single-turn
  control excluded-with-reason; κ scoped to main run; adopted-X strict metric reported.
- ✅ Structural check: environments balanced, all \ref/\cite resolve, all figure files present.
- ⛔ **NOT COMPILED** — no LaTeX toolchain on this machine (`pdflatex`/`tectonic` absent).
  Compile once on Overleaf (upload `paper/` folder) or `brew install --cask mactex-no-gui`.
  Expect only cosmetic fixes (float placement, hbox warnings).
- ⚠️ ~4,700 words + 7 figures + 2 tables ≈ 8–9 typeset pages — within IEEE Access norms.

## Authorship / admin (only you can do these)
- ⛔ Author names, order, affiliations, e-mails, ORCIDs (`TODO-AUTHORS` markers in access.tex).
- ⛔ Author biographies + photos (template placeholders commented at end of access.tex).
- ⛔ Funding / acknowledgment footnote (`\tfootnote`).
- ⛔ APC ~US$1,995–2,160 — confirm funding source; check IEEE APC waiver/discount eligibility
  (some institutions have agreements; IEEE offers discounts for some countries).
- ⚠️ Advisor sign-off if this is a college project (authorship norms).

## Reproducibility package
- ✅ Pre-registration cited with commit hash (427d75a, 2026-06-29) + deviations table
  (DEVIATIONS_FROM_PREREGISTRATION.md, extended by the deviations listed in Method §D).
- ✅ RESULTS_MANIFEST.sha256 (24/24 verified this session); raw JSONL + judge outputs +
  human-validation labels in repo.
- ⚠️ **Recommended before submission**: deposit a frozen snapshot on Zenodo → DOI; cite the DOI in
  the Data Availability section (stronger than a GitHub link; GitHub repos can move).
- ⚠️ Consider a clean `paper-repo` (curated: pre-reg, deviations, data, src, figures, README) so
  reviewers don't wade through worklogs. Current repo works but is noisy.
- ✅ Dataset licensing: ARC-Challenge (CC BY-SA), MMLU (MIT), TruthfulQA (Apache 2.0) all permit
  redistribution with attribution — attributed in Data Availability. (Verify the exact license
  strings when building the clean repo README.)

## Statistics / integrity
- ✅ BH correction run (pre-registered family E1/E2/E4/E5; post-hoc McNemar family separate) —
  was pre-registered but never executed until this pass; now in paper_numbers.py.
- ✅ E2 domain moderation computed (LRT χ²₂=6.96, q=0.039) — new exploratory result, reported.
- ⚠️ E6 (external benchmark calibration) NOT run — disclosed in Limitations + deviations. Optional.
- ⚠️ E3 (hedging language) descriptive only — disclosed. Optional.
- ⛔→⚠️ **Single-turn judge pass** (~6,880 trials, one Gemini API evening, `judge_bundle/src/run_judge.py`
  adapted to `*_singleturn.jsonl`): the manuscript ships with κ scoped to the main run, so this is
  NOT a hard blocker — but it is the single cheapest robustness add and kills a likely reviewer ask.
  Strongly recommended before submission.

## Figures
- ✅ New: fig9 design schematic, fig10 commitment penalty (headline), fig11 confidence — generated
  from paper_numbers.json via committed `src/make_figures_paper.py`, visually inspected.
- ✅ Reused per FIGURE_AUDIT: fig1 (null), fig2 (ladders), fig3 (persona A/B), fig8 (direction);
  fig4 CUT (circular); fig5/6/7 not in manuscript (fig6 ΔH numbers reported in text instead).
- ⚠️ fig1/fig2/fig3/fig8 were generated by earlier scripts (`make_figures*.py`) — captions in the
  manuscript now carry the FIGURE_AUDIT discipline (n, Wilson, gated, exploratory), but the PNGs
  themselves still carry their original titles. Cosmetic; regenerate without embedded titles if
  you want polish (IEEE prefers caption-only titling).
- ⚠️ IEEE prefers vector (EPS/PDF) figures; PNGs at 300dpi are acceptable. Optional: re-export as PDF.

## Originality / plagiarism safety
- ✅ Manuscript prose written fresh this session; not pasted from internal .md notes (which are
  themselves unpublished — no self-plagiarism exposure). iThenticate risk: low. The one reused
  sentence-shape is the fab_inference "safe wording" for the single-turn trend — internal, fine.

## Submission mechanics (IEEE Author Portal)
- ⚠️ Suggested reviewers (3–5, no conflicts) — prepare; consider authors of the cited sycophancy
  benchmark papers not in direct competition.
- ⚠️ Cover letter — one page: pre-registered null + commitment factorial framing; state that all
  data/code are public.
- ⚠️ ORCID mandatory for corresponding author; IEEE PDF eXpress check after compile.
- ⚠️ Graphical abstract (optional but favored): fig10 works as-is.

## Recommended order of operations
1. Compile on Overleaf → fix cosmetics. 2. Fill authors/ORCID/bios/funding. 3. Run single-turn
judge evening + re-run `paper_numbers.py` (add judge-coverage sentence). 4. Zenodo DOI + clean
paper-repo. 5. PDF eXpress + submit.
